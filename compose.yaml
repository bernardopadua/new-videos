services:
    postgres:
        image: postgres:16
        shm_size: 128mb
        env_file:
            - .env.postgres
        volumes:
            - ./.data:/var/lib/postgresql/data
        ports:
          - "5432:5432"
        networks:
            nvideos_net:
                ipv4_address: 172.12.0.16

networks:
  nvideos_net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.12.0.0/16